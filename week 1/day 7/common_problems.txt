some of the common problems i found hard to remember 


#question 7 
#reshape into a 2 x 3 array 
numbers = np.array([1, 2, 3, 4, 5, 6])

#i found this one hard to remmeber 
#you need to use the reshape() method 


numbers = np.array([1, 2, 3, 4, 5, 6])

reshaped = numbers.reshape(2, 3)

print(reshaped)
print(reshaped.shape)

#so we use reshape(rows, columns) builtin method 







#question 10 

transpose , you forgot this ! 

#so we use .T 
#this is the syntax 

#remember it flips



matrix = np.array([
    [1, 2, 3],
    [4, 5, 6]
])

transposed = matrix.T

print(transposed)
print(transposed.shape)


[[1 4]
 [2 5]
 [3 6]]

(3, 2)



axis=1 means the columns are collapsed into one value per row:

[10, 20, 30] → [60]
[40, 50, 60] → [150]



axis=0 collapses the rows, leaving one result for each column.

Collapsed means: multiple values along one axis are combined into one value.


remember shuffle 

numbers = np.array([10, 20, 30, 40, 50])
rng = np.random.default_rng(7)

rng.shuffle(numbers)

print(numbers)
print(numbers.shape)